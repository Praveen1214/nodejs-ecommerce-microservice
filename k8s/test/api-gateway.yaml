apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: ecommerce-test
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-gateway
      environment: test
  template:
    metadata:
      labels:
        app: api-gateway
        environment: test
    spec:
      containers:
        - name: api-gateway
          image: abhisheklpeiris/api-gateway-service:d572329
          imagePullPolicy: Always
          ports:
            - containerPort: 3003
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          env:
            - name: PORT
              value: "3003"
            - name: NODE_ENV
              value: "test"
            - name: AUTH_SERVICE_URL
              value: "http://auth:3000"
            - name: PRODUCT_SERVICE_URL
              value: "http://product:3001"
            - name: ORDER_SERVICE_URL
              value: "http://order:3002"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: JWT_SECRET
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: ecommerce-test
  labels:
    app: api-gateway
    environment: test
spec:
  type: LoadBalancer
  selector:
    app: api-gateway
    environment: test
  ports:
    - name: http
      port: 3003
      targetPort: 3003
